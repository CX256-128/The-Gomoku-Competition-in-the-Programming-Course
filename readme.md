# Gomoku YES!!

I created two program in the train_data: transformer.cpp and reader.cpp
transformer.cpp   : is used to compose the data and reattribute them
reader.cpp        : is used to visualize every step in the data
